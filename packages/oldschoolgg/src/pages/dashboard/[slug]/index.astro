---
export const prerender = false;

import { DashboardPageWrapper } from '../../../components/Dashboard/DashboardPageWrapper.tsx';
import { SettingsPage } from '../../../components/Dashboard/SettingsPage.tsx';
import Layout from '../../../layouts/Layout.astro';

const { slug } = Astro.params;
console.log('slug:', slug);
console.log('INDEX');
---

<Layout>
	<DashboardPageWrapper client:only="react" slug={slug} pageTitle="Settings">
		{(props: any) => <SettingsPage client:only="react" {...props} />}
	</DashboardPageWrapper>
</Layout>
