import { Emoji } from '../../../constants';
import { SkillsEnum } from '../../types';

const Magic = {
	aliases: ['mage', 'magic'],
	id: SkillsEnum.Magic,
	emoji: Emoji.Magic,
	name: 'Hunter'
};

export default Magic;
